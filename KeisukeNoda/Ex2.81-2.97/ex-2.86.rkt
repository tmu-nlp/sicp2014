#lang racket

I need to modify the (install-complex-package) to use add instead of +, mul instead of *, etc. Then the generic ops will be used.

The sine&cosine should be add to both rational and scheme-number packages as they are used only on that level. 


